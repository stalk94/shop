export type Tovar = {
    id: number
    code: string
    count: number
    name: string
    description: string
    image: string
    price: number
    category: string
    status: boolean
    timeshtamp: string|number
}